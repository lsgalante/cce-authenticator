[package]
name = "cce-authenticator"
version = "0.1.0"
edition = "2021"

[dependencies]
cce-ui = { git = "https://github.com/lsgalante/cce-ui.git", rev = "a530574f7fa1f0938049db92053a17df27756acb" }
# The raw PAM binding, for the standalone password check. The higher-level `pam`
# crate was declared next to it and never called.
pam-sys = "0.5.6"
libc = "0.2"
# passwd lookups: our own identity, and the uid polkit names in its identity list.
users = "0.8.1"
calloop = "0.13.0"
wayland-client = { version = "0.31", features = ["system"] }
tokio = { version = "1", features = ["full"] }
zbus = "5"
futures = "0.3"
log = "0.4"
env_logger = "0.11"
